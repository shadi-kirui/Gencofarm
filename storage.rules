rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Anyone can write
      allow write: if true;

      // Only authenticated users can read
      allow read: if true;
    }
  }
}