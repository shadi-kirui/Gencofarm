{
  "name": "genco-export-functions",
  "private": true,
  "version": "1.0.0",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && npx firebase emulators:start --only functions",
    "deploy": "npx firebase deploy -project genco-export",
    "logs": "npx firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.2.5",
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.1",
    "typescript": "^5.6.0"
  }
}
